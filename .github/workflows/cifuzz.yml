name: CIFuzz
on:
  pull_request:
    paths:
      - '**'
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
 BatchFuzzing:
   runs-on: ubuntu-latest
   steps:
   - name: echo env vars
     run: echo "GITHUB_HEAD_REF $GITHUB_HEAD_REF GITHUB_BASE_REF $GITHUB_BASE_REF GITHUB_REF $GITHUB_REF GITHUB_HEAD_REF $GITHUB_HEAD_REF GITHUB_ACTION_REF $GITHUB_ACTION_REF GITHUB_SHA $GITHUB_SHA GITHUB_RUN_ID $GITHUB_RUN_ID"
   - name: Build Fuzzers
     id: build
     #uses: google/clusterfuzzlite/actions/build_fuzzers@main
     uses: google/oss-fuzz/infra/cifuzz/external-actions/build_fuzzers@fix-coverage
     with:
       storage-repo: https://${{ secrets.ACCESS_TOKEN }}@github.com/oliverchang/cifuzz-external-storage.git
       storage-repo-branch: main
       storage-repo-branch-coverage: gh-pages
   - name: Run Fuzzers
     id: run
     #uses: google/clusterfuzzlite/actions/run_fuzzers@main
     uses: google/oss-fuzz/infra/cifuzz/external-actions/run_fuzzers@fix-coverage
     with:
       fuzz-seconds: 600
       run-fuzzers-mode: 'batch'
       storage-repo: https://${{ secrets.ACCESS_TOKEN }}@github.com/oliverchang/cifuzz-external-storage.git
       storage-repo-branch: main
       storage-repo-branch-coverage: gh-pages
 Coverage:
   runs-on: ubuntu-latest
   steps:
   - name: echo env vars
     run: echo "GITHUB_HEAD_REF $GITHUB_HEAD_REF GITHUB_BASE_REF $GITHUB_BASE_REF GITHUB_REF $GITHUB_REF GITHUB_HEAD_REF $GITHUB_HEAD_REF GITHUB_ACTION_REF $GITHUB_ACTION_REF GITHUB_SHA $GITHUB_SHA GITHUB_RUN_ID $GITHUB_RUN_ID"
   - name: Build Fuzzers
     id: build
     #uses: google/clusterfuzzlite/actions/build_fuzzers@main
     uses: google/oss-fuzz/infra/cifuzz/external-actions/build_fuzzers@fix-coverage
     with:
       sanitizer: coverage
       storage-repo: https://${{ secrets.ACCESS_TOKEN }}@github.com/oliverchang/cifuzz-external-storage.git
       storage-repo-branch: main
       storage-repo-branch-coverage: gh-pages
   - name: Run Fuzzers
     id: run
     #uses: google/clusterfuzzlite/actions/run_fuzzers@main
     uses: google/oss-fuzz/infra/cifuzz/external-actions/run_fuzzers@fix-coverage
     with:
       fuzz-seconds: 600
       run-fuzzers-mode: 'coverage'
       storage-repo: https://${{ secrets.ACCESS_TOKEN }}@github.com/oliverchang/cifuzz-external-storage.git
       storage-repo-branch: main
       storage-repo-branch-coverage: gh-pages
 Prune:
   runs-on: ubuntu-latest
   steps:
   - name: echo env vars
     run: echo "GITHUB_HEAD_REF $GITHUB_HEAD_REF GITHUB_BASE_REF $GITHUB_BASE_REF GITHUB_REF $GITHUB_REF GITHUB_HEAD_REF $GITHUB_HEAD_REF GITHUB_ACTION_REF $GITHUB_ACTION_REF GITHUB_SHA $GITHUB_SHA GITHUB_RUN_ID $GITHUB_RUN_ID"
   - name: Build Fuzzers
     id: build
     #uses: google/clusterfuzzlite/actions/build_fuzzers@main
     uses: google/oss-fuzz/infra/cifuzz/external-actions/build_fuzzers@fix-coverage
     with:
       build-integration-path: 'fuzzer-build-integration'
       oss-fuzz-project-name: ''
   - name: Run Fuzzers
     id: run
     #uses: google/clusterfuzzlite/actions/run_fuzzers@main
     uses: google/oss-fuzz/infra/cifuzz/external-actions/run_fuzzers@fix-coverage
     with:
       oss-fuzz-project-name: ''
       build-integration-path: 'fuzzer-build-integration'
       fuzz-seconds: 600
       run-fuzzers-mode: 'prune'
       storage-repo: https://${{ secrets.ACCESS_TOKEN }}@github.com/oliverchang/cifuzz-external-storage.git
       storage-repo-branch: main
